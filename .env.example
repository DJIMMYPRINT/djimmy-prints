# Supabase project used to store orders (server-only — the service role key
# must never be prefixed NEXT_PUBLIC_ and never sent to the browser)
SUPABASE_URL=https://ivxvzyokijsatdlonpec.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
